{% extends 'base.html.twig' %}



{% block body %}
    <h1>Hospedaje</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hospedaje.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ hospedaje.tipo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hospedaje_index') }}">back to list</a>

    <a href="{{ path('hospedaje_edit', {'id': hospedaje.id}) }}">edit</a>

    {{ include('hospedaje/_delete_form.html.twig') }}
{% endblock %}
