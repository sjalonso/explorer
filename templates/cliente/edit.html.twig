{% extends 'base.html.twig' %}



{% block body %}

    <section class="content">
        
        <div class="row">
            {{ include('cliente/_form.html.twig', {'button_label': 'ACTUALIZAR'}) }}

            <a href="{{ path('cliente_index') }}"><button class="btn btn-success" style="margin: 0px 5px">VOLVER AL LISTADO</button></a>
            <p>
            {{ include('cliente/_delete_form.html.twig') }}

        </div>
    </section>
{% endblock %}
