{% extends 'base.html.twig' %}



{% block body %}
    <section class="content">
        <!-- Small boxes (Stat box) -->
        <div class="box">
            <div class="box-header">
              <h3 class="box-title">Listas Aerolineas</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
    <table id="example1" class="table table-bordered table-striped" width="100%">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for aerolinea in aerolineas %}
            <tr>
                <td>{{ aerolinea.nombre }}</td>
                <td>
                <a href="{{ path('aerolineas_show', {'id': aerolinea.id}) }}"><button class="fa fa-eye btn-sm"></button></a>
                {% if is_granted("ROLE_AEROLINEA" ) or is_granted("ROLE_ADMIN" ) %}
                <a href="{{ path('aerolineas_edit', {'id': aerolinea.id}) }}"><button class="fa fa-pencil btn-sm"></button></a>
                  {% endif %}  
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
  </div>
        </div>
        {% if is_granted("ROLE_AEROLINEA" ) or is_granted("ROLE_ADMIN" ) %}
        <a href="{{ path('aerolineas_new') }}"><button class="btn btn-primary">CREAR NUEVA AEROLÍNEA</button></a>
          {% endif %}  
{% endblock %}
