
<div class="col-md-12">
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Gestionar Ruta</h3>
        </div>
        <!-- /.box-header -->
        <div class="row">
            <div class="col-md-12">
                <div class="box-body">
                    {{ form_start(form) }}
                  
                    {#Fila1#}
                    <div class="row">
                        <div class="col-md-3">
                            <div class="form-group">
                            Aeropuertos
                            {{ form_widget(form.areopuertos, {'attr': {'class': 'form-control'}}) }}
                            </div>
                        </div>

                        <div class="col-md-3">
                            <div class="form-group">
                                Nombre de rutas ej(Habana-Haiti)
                                {{ form_widget(form.nombreruta, {'attr': {'class': 'form-control'}}) }}
                            </div>
                        </div>

                                
                    </div>
                    {#Fin fila 1#}

                      


                    <div style="float: right">
                        <button class="btn btn-primary" style="margin: 0px 5px">{{ button_label|default('ADICIONAR') }}</button>
                        
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
