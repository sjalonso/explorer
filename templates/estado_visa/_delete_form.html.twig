<form method="post" action="{{ path('estado_visa_delete', {'id': estado_visa.id}) }}" onsubmit="return confirm('¿Está seguro que desea eliminar este registro?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estado_visa.id) }}">
   <button class="btn btn-danger" style="margin: 0px 5px">ELIMINAR</button>
</form>
