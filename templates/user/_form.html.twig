<div class="col-md-4">
    <div class="box">
        <div class="box-header">
            {#<h3 class="box-title">Adicionar Usuario</h3>#}
        </div>
        <!-- /.box-header -->
        <div class="row">
            <div class="col-md-12">
                <div class="box-body">
                    {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <div style="float: right">
                            <button class="btn btn-primary" style="margin: 0px 5px">{{ button_label|default('GUARDAR') }}</button>
                            <a href="{{ path('home') }}"><button class="btn btn-danger" style="margin: 0px 5px">CANCELAR</button></a>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>

