{% extends 'base.html.twig' %}



{% block body %}

    <section class="content">
        
        <div class="row">
            {{ include('estado_reserva/_form.html.twig', {'button_label': 'ACTUALIZAR'}) }}

            <a href="{{ path('estado_reserva_index') }}"><button class="btn btn-success" style="margin: 0px 5px">VOLVER AL LISTADO</button></a>
            <p>
            {{ include('estado_reserva/_delete_form.html.twig') }}

        </div>
    </section>
{% endblock %}
