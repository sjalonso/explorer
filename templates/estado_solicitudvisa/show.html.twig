{% extends 'base.html.twig' %}


{% block body %}
    <h1>EstadoSolicitudvisa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estado_solicitudvisa.id }}</td>
            </tr>
            <tr>
                <th>Tipoestado</th>
                <td>{{ estado_solicitudvisa.tipoestado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estado_solicitudvisa_index') }}">back to list</a>

    <a href="{{ path('estado_solicitudvisa_edit', {'id': estado_solicitudvisa.id}) }}">edit</a>

    {{ include('estado_solicitudvisa/_delete_form.html.twig') }}
{% endblock %}
