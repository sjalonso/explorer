{% extends 'base.html.twig' %}



{% block body %}
    <h1>EstadoSolicitudvisa index</h1>

    <table class="table" width="100%">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tipo de estado</th>

                <th>acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for estado_solicitudvisa in estado_solicitudvisas %}
            <tr>
                <td>{{ estado_solicitudvisa.id }}</td>
                <td>{{ estado_solicitudvisa.tipoestado }}</td>
                <td>
                    <a href="{{ path('estado_solicitudvisa_show', {'id': estado_solicitudvisa.id}) }}">show</a>
                    <a href="{{ path('estado_solicitudvisa_edit', {'id': estado_solicitudvisa.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('estado_solicitudvisa_new') }}">Create new</a>
{% endblock %}
