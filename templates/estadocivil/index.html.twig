{% extends 'base.html.twig' %}



{% block body %}
     <section class="content">
        <!-- Small boxes (Stat box) -->
        <div class="box">
            <div class="box-header">
              <h3 class="box-title">Listas de Estados Civil</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
                <table id="example1" class="table table-bordered table-striped" width="100%">
                    <thead>
                        <tr>
            <tr>
            <tr>
                <th>Nombre</th>
                <th>accciones</th>
            </tr>
        </thead>
        <tbody>
        {% for estadocivil in estadocivils %}
            <tr>
                <td>{{ estadocivil.tipoestado }}</td>
                <td>
                    <a href="{{ path('estadocivil_show', {'id': estadocivil.id}) }}"><button class="fa fa-eye btn-sm"></button></a>
                    <a href="{{ path('estadocivil_edit', {'id': estadocivil.id}) }}"><button class="fa fa-pencil btn-sm"></button></a>
                </td>
            </tr>
        {% else %}
            <td colspan="3">No se encontraron registros</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        <a href="{{ path('estadocivil_new') }}"><button class="btn btn-primary">CREAR NUEVO ESTADO CIVIL</button></a>
    </section>
    {% endblock %}